"""Agent factories — one file per specialist that reasons over tools rather
than executing a fixed sequence (docs/PLAN.md §9.3): `network.py`,
`billing.py`, `account.py`, `escalation.py`, added starting Phase 3.
"""
